# 🎵 SoundUniverse ML Service

Сервис на базе Machine Learning для анализа музыкальных треков и определения жанра.
Проект упакован в Docker и предоставляет API (FastAPI).

## 📋 Требования

Для запуска вам понадобятся:
- **Docker Desktop** (установленный и запущенный)
- **Git**
- Папка с датасетом (структура описана ниже)

## 📂 Подготовка данных

Сервис ожидает, что вы подключите к нему папку с данными.
Создайте на своем компьютере папку (например, `MyMusicData`) со следующей структурой:

```text
MyMusicData/
├── raw_dataset/       # Сюда положить папки с музыкой (жанры)
├── processed_data/    # (Создастся автоматически)
├── models/            # (Создастся автоматически, сюда сохраняются веса)
├── logs/              # (Сюда пишутся логи)
└── graphics/          # (Сюда сохраняются графики)
```

# Инструкция по запуску
1. Создайте проект в IDE и скопируйте репозиторий
```code
git clone https://github.com/SoundUniversal/ml_service
cd SoundUniverse-ML-Service
```
2. Откройте файл docker-compose.yml и замените путь к данным на ваш
```code
volumes:
- D:\SoundUniverse_ML:/data
```
3. Запустите сервис
```code
docker-compose up --build
```
После завершения сборки сервис будет доступен по локальному ip - http://localhost:8000/docs
